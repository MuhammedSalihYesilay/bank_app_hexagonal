services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=bank_app_hexagonal'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=user'
    ports:
      - '5432:5432'
